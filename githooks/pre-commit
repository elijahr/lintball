#!/usr/bin/env bash

set -ueo pipefail

# path to lintball dir
GITHOOKS_DIR="$(dirname "${BASH_SOURCE[0]}")"

# path to repo/package
LINTBALL_DIR="$(
  cd "${GITHOOKS_DIR}/.."
  pwd
)"

"${LINTBALL_DIR}/bin/lintball" pre-commit
